en:
# English language.

  js:
  # JavaScript

    discourse_automation:
      triggerables:
	# The new triggerable action. 
        send_email_on_new_post:  
          fields:
	  # configurable parameters or inputs
            email_recipient:
              label: "Recipient Email"
            email_subject:
              label: "Email Subject"
            email_body:
              label: "Email Body"
